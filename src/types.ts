export declare type ImageType =
  | HTMLImageElement
  | HTMLVideoElement
  | HTMLCanvasElement;

  export declare interface Padding {
    top: number;
    bottom: number;
    left: number;
    right: number;
  }
